
sudo: required
addons:
  chrome: stable

language: node_js
node_js:
  - "8"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - npm install
  - ng build --prod --aot
  - ng test

deploy:
  provider: heroku
  api_key:
    secure: "We1DBQTI5bMOQWgrsfRb2A"